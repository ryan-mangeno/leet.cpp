### Linux, Bash & Git Knowledge Check

**Your Task**
Implement the functions in `Submission` to prove your familiarity with the command line.
Return the exact command or flag required.

**Questions:**
1. **Git:** Which command stages changes for the next commit?
2. **Bash:** Which operator redirects `stderr` to the same place as `stdout`?
3. **Process:** What is the difference between a process and a thread? (Return "memory" if processes have separate memory, "shared" if they share it).
4. **Linux:** Which command shows the current running processes?
