#pragma once
#include <string>

namespace leet_cpp {

class LinuxQuiz {
public:
    // Q1: Git command to stage files (e.g. "git ???")
    static std::string git_stage_command() {
        return ""; // TODO
    }

    // Q2: Bash operator to redirect stderr to stdout (e.g. ">>", "|", etc)
    static std::string bash_redirect_stderr_to_stdout() {
        return ""; // TODO
    }

    // Q3: Do processes share memory space by default? Return "yes" or "no".
    static std::string process_memory_sharing() {
        return ""; // TODO
    }
    
    // Q4: Command to list running processes (just the 2 letter command)
    static std::string list_processes_cmd() {
        return ""; // TODO
    }
};

}
